# Environment / credentials — never commit these
.env
.env.local
.env.*.local

# Netlify local dev cache
.netlify/

# Node modules (if ever added)
node_modules/

# Local Claude Code preview/launch config (developer-specific)
.claude/
