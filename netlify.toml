[build]
  publish = "."
  command = "echo 'Static site — no build step required'"

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# ── Security headers ──────────────────────────────────────
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# ── Cache: static assets (long TTL) ──────────────────────
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ── Cache: HTML pages (short TTL — updated during festivals) ─
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=300, stale-while-revalidate=60"

# ── Cache: JS modules (short TTL — updated during festivals) ─
[[headers]]
  for = "/js/*.js"
  [headers.values]
    Cache-Control = "public, max-age=300, stale-while-revalidate=60"

# ── Redirects ──────────────────────────────────────────────
# Clean URL: /cheltenham-2026 → /cheltenham-2026.html
[[redirects]]
  from = "/cheltenham-2026"
  to   = "/cheltenham-2026.html"
  status = 200

[[redirects]]
  from = "/grand-national-2026"
  to   = "/grand-national-2026.html"
  status = 200

[[redirects]]
  from = "/cheltenham-2027"
  to   = "/cheltenham-2027.html"
  status = 200

[[redirects]]
  from = "/season-2026"
  to   = "/season-2026.html"
  status = 200

[[redirects]]
  from = "/chester-2026"
  to   = "/chester-2026.html"
  status = 200

[[redirects]]
  from = "/dante-2026"
  to   = "/dante-2026.html"
  status = 200

[[redirects]]
  from = "/ffos-las-2026"
  to   = "/ffos-las-2026.html"
  status = 200

[[redirects]]
  from = "/epsom-derby-2026"
  to   = "/epsom-derby-2026.html"
  status = 200

# Future festivals — add a redirect here each time you add a new file
# [[redirects]]
#   from = "/royal-ascot-2026"
#   to   = "/royal-ascot-2026.html"
#   status = 200

# Fallback: anything unknown → hub homepage
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 302
